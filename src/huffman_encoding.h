#ifndef HUFFMAN_ENCODING_H
#define HUFFMAN_ENCODING_H

#include<vector>

int bit_size(int);
void huffman_encode( int[64], int*, std::vector<char>& );

/*
struct huffman_code{
	int size;
	int value;
} huffman_code;
*/
#endif
